# The production URL of this site, with no trailing slash.
# Used for canonical links, OG/Twitter tags, JSON-LD URLs, and sitemap generation.
# Copy this file to .env and set the real value before a production deploy.
# Falls back to https://example.com if unset (fine for local dev).
SITE_URL=https://example.com
